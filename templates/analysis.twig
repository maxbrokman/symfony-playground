SamKnows Metric Analyser v1.0.0
===============================

Period checked:

    From: {{ set.dateRangeStart.format("Y-m-d") }}
    To:   {{ set.dateRangeEnd.format("Y-m-d") }}

Statistics:

    Unit: Megabits per second

    Average: {{ set.average|megabitsFromBytes }}
    Min: {{ set.minimum|megabitsFromBytes }}
    Max: {{ set.maximum|megabitsFromBytes }}
    Median: {{ set.median|megabitsFromBytes }}
{% if set.lowOutlierSets|length > 0 %}

Investigate:

{% for low in set.lowOutlierSets %}
    * The period between {{ low.start.format("Y-m-d") }} and {{ low.end.format("Y-m-d") }}
      was under-performing.
{% endfor %}

{% endif %}
